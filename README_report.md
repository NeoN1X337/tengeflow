# TengeFlow - Финальный технический отчет

## 📋 Стек технологий

- **Frontend Framework**: React (Vite)
- **Styling**: Tailwind CSS v3.4.17 + Flowbite React
- **Backend/Auth**: Firebase (Authentication + Firestore)
- **Icons**: Lucide-React
- **Charts**: Recharts (Analytics)
- **Testing**: Playwright (E2E)
- **Routing**: React Router DOM
- **Deployment**: Vercel (planned)

> **Важно**: Изначально планировался Tailwind v4, но был выполнен downgrade до v3.4.17 из-за несовместимости с Flowbite React.

---

## ✅ Текущий статус проекта

### Реализованный функционал

#### Аутентификация
- ✅ Вход через Email/Password и Google OAuth
- ✅ Регистрация с подтверждением пароля и Email Verification
- ✅ **Security**: Сброс пароля (Reset Password) и защита от входа без верификации
- ✅ **UX**: Индикатор сложности пароля (Strength Meter) и переключение видимости (Eye Icon)
- ✅ Protected routes и глобальное управление сессией

#### База данных и Транзакции
- ✅ Custom hook `useTransactions.js` с полным CRUD и оптимистичными обновлениями
- ✅ Firestore Real-time Sync (мгновенное отображение изменений)
- ✅ **Система фильтрации**: Тип, Категория, Налогооблагаемость, Месяц
- ✅ **Налоги**: Глобальная ставка налога обновлена до **4%** (ранее 3%)

#### UI/UX
- ✅ **Dashboard**: Баланс, быстрые фильтры, последние операции
- ✅ **Analytics**:
  - ✅ Графики Recharts (PieChart расходов, BarChart трендов)
  - ✅ **Tax Calendar v1.5**: Детальная таблица налогов (Q1-Q4, H1-H2) с расчетом по выбранному году
  - ✅ **Period Context**: Глобальный выбор Года/Месяца в шапке Dashboard и Аналитики
  - ✅ **Decimal Precision**: Поддержка тиынов (2 знака после запятой) во всех расчетах и интерфейсах
- ✅ **Smart Notifications**: Подтверждающие Toast-уведомления для всех операций. Умная логика на Dashboard: уведомление подскажет, если новая операция с "задней датой" не попала в список 5 последних.
- ✅ Унифицированные компоненты `TransactionItem`, `FilterBar`, `TransactionModal`
- ✅ Адаптивный дизайн (Mobile First)

### 🧪 E2E Тестирование (New in v1.4)
Внедрен полный цикл End-to-End тестирования на **Playwright**:
- ✅ **Инфраструктура**: Настроен `playwright.config.js` с видео и скриншотами при падении.
- ✅ **Изоляция**: Тесты запускаются в изолированных контекстах браузера.
- ✅ **Test Refactoring**:
  - Внедрены атрибуты `data-testid` во все ключевые компоненты для надежности.
  - Устранены "Race Conditions" (ожидание загрузки Firebase).
  - Использование строгих проверок (`toBeHidden`, `toBeVisible`).
- ✅ **Scenarios**:
  - `basic.spec.js`: Базовая проверка входа и создания транзакции.
  - `full-cycle.spec.js`: Полный пользовательский путь (Login -> Action -> Dashboard -> Analytics Check).
  - ✅ **Auth Tests**: Верификация флоу регистрации, проверки сложности пароля и редиректа на страницу подтверждения почты.

---

## 🐛 Решенные баги (v1.3 - v1.4)

### 1. Тестирование и Стабильность
- ✅ **Modal Race Condition**: Тесты падали при быстром закрытии модалки. Исправлено добавлением `await expect(...).toBeHidden()`.
- ✅ **Strict Mode Violations**: Исправлена ошибка дублирующихся оверлеев Flowbite путем использования `.first()` локаторов.
- ✅ **Currency Parsing**: Тесты падали из-за неразрывных пробелов в форматировании валют. Внедрены Regex-проверки `/50.*000/`.

### 2. Фильтрация и Данные
- ✅ **Balance Calculation**: Исправлена логика подсчета баланса при отрицательных значениях в тестах.
- ✅ **Tax Rate**: Успешная миграция всех расчетов и UI на налоговую ставку 4%.
- ✅ **Transaction Sorting**: Исправлен баг "хаотичной" сортировки внутри одного дня. Внедрена вторичная сортировка по `createdAt` в `useTransactions.js`.

### 3. UI/UX и Рендеринг
- ✅ **Recharts Resize**: Устранены предупреждения в консоли `The width(-1) and height(-1) of chart should be greater than 0`. Добавлен `debounce={200}` и `width="99%"`.
- ✅ **Analytics Crash**: Исправлена критическая ошибка `ReferenceError: period is not defined` при переключении фильтров.
- ✅ **Precision Loss**: Удалено принудительное округление `Math.ceil` в `useTransactions.js` и `taxUtils.js`, что позволило видеть точные суммы с копейками.
- ✅ **Input Validation**: Установлен минимальный порог транзакции в 1 ₸ для предотвращения мусорных данных.
- ✅ **Email Regex**: Внедрена строгая валидация почты (запрет 1-символьных доменов вроде `1@d.com`).
- ✅ **Empty State Crash**: Исправлена ошибка в `TransactionModal`, вызывавшая падение при добавлении первой транзакции в пустой аккаунт.

---

## 🔍 Структура проекта

### Ключевые файлы

```
src/
├── components/
│   ├── AuthForm.jsx             # Форма входа/регистрации (data-testid ready)
│   ├── FilterBar.jsx            # Панель фильтров
│   ├── TransactionItem.jsx      # Строка транзакции
│   ├── TransactionModal.jsx     # Модальное окно (Add/Edit)
│   ├── Sidebar.jsx              # Боковое меню
│   ├── Toast.jsx                # Глобальные уведомления
│   └── MainLayout.jsx           # Обертка приложения
├── contexts/
│   ├── AuthContext.jsx          # Управление сессией
│   └── NotificationContext.jsx  # Система уведомлений
├── hooks/
│   └── useTransactions.js       # Логика Firestore и расчетов
├── pages/
│   ├── Dashboard.jsx            # [UPDATED] Главная с фильтром по периодам
│   ├── Transactions.jsx         # История операций (очищена от лишних фильтров)
│   ├── Analytics.jsx            # [UPDATED] Аналитика с Налоговым Календарем
│   └── Profile.jsx              # Профиль
└── firebase.js                  # Конфигурация SDK

tests/
└── e2e/
    └── full-cycle.spec.js       # [CONSOLIDATED] Полный сценарий с проверкой копеек и лет
```

---

## 📝 Заключение

Проект **TengeFlow** обновлен до версии **1.6 (Auth Upgrade & Smart UX)**.

**Ключевые достижения v1.6 (Session Jan 19):**
1.  **Безопасность Auth**: Внедрена обязательная верификация Email и сброс пароля. Теперь приложение защищено от входа с неподтвержденных адресов.
2.  **Валидация 2.0**: Пароли теперь проверяются на сложность (8+ знаков, спецсимволы, регистр) с визуальным Strength Meter. Email проверяется строгим Regex.
3.  **Smart UX**: Реализована система контекстных уведомлений. Больше нет "тихих" сохранений — пользователь всегда получает подтверждение действия. Умная логика добавления транзакций предотвращает путаницу, если операция не появляется в списке сразу.
4.  **Стабильность**: Исправлены критические ошибки при первом запуске (пустая база) и улучшена архитектура модальных окон.

**Дата последнего обновления**: 2026-01-19
**Версия**: 1.6 (Auth Upgrade & Smart UX)
**Статус**: Production Ready / Enhanced Security